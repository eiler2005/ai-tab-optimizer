{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_FILE_PATHS\" | grep -qE '\\.(ts|tsx)$'; then cd extension && pnpm typecheck 2>&1 | tail -5; fi",
            "timeout": 30000
          }
        ]
      }
    ]
  }
}
